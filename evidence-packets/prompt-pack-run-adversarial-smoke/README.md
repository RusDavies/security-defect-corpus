# Prompt-Pack Run Adversarial Smoke

Negative-control run-level evidence for the AI-agent prompt-pack rubric. This
packet represents a run that finds some defects but ships unsafe repair evidence,
skips approval, omits residual risk, and leaves coverage gaps.
